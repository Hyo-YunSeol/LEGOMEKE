# 레고 키우기 v6.4.2 최종 검증

검증 기준: v6.4.1 최종본에 오목판 정사각형 수정과 운영자 회원 계정 완전 삭제 기능을 추가한 v6.4.2.

## 자동 검증 결과

`npm run verify`를 최종 작업 트리에서 3회 연속 실행했습니다.

- JavaScript 문법 검사: 33개 파일 통과
- Node 테스트: 159개
- 통과: 159
- 실패: 0
- 취소: 0
- 건너뜀: 0

테스트 중 잘못된 JSON 요청을 의도적으로 보내 직렬 처리 큐가 오류 뒤에도 계속 동작하는지 확인하는 케이스가 있어 콘솔에 `JSON 형식이 올바르지 않습니다.` 로그가 한 번 출력될 수 있습니다. 해당 케이스 자체는 정상 통과하며 최종 실패 수는 0입니다.

## v6.4.2 신규 검증 항목

### 오목판

- CSS Grid 15열 확인
- CSS Grid 15행 확인
- 보드 `aspect-ratio: 1 / 1` 확인
- 보드 `box-sizing: border-box` 확인
- 각 오목 셀 width/height 100% 확인
- 기존 착수/관전/재대결 UI 유지 확인
- 기존 오목 규칙/정산 전체 회귀 테스트 통과

### 운영자 계정 삭제

- 일반 사용자 삭제 API 호출 403
- 운영자 본인 계정 삭제 차단
- 존재하는 다른 회원 삭제 성공
- 현재 세대 + 과거 세대 레고 전부 제거
- 삭제 user 레코드 제거
- 관계/찌르기/요청/개인게임 상태 참조 제거
- 삭제된 계정의 기존 토큰 인증 실패
- 삭제된 계정의 기존 닉네임/PIN 로그인 실패
- 삭제 후 같은 닉네임 신규 가입 가능
- 오목 진행 중 계정 삭제 시 상대 승리/판돈/승패 기록 1회 정산
- 라이어게임 진행 중 계정 삭제 시 안전 종료 및 남은 플레이어 판돈 반환

## 기존 기능 회귀 검증

159개 전체 테스트에는 개인게임 30회, 사과게임, 낚시 20회, 홀짝, 영토전 황금 위치 비공개, 오목 금수/판돈/시간초과/관전/랭킹, 라이어 관전/비밀정보 차단, 관리자 포인트 지급·회수, 회원가입/로그인, 생활/관계/벙/파손/세대교체 등이 함께 포함됩니다.

## 실제 기기 검증 범위

이 환경에서는 실제 iPhone/Android 브라우저 화면을 손으로 조작해 물리적인 렌더링을 확인할 수 없습니다. 따라서 오목판 정사각형은 CSS 구조와 자동 정적 검증으로 확인했습니다. Cloudflare 배포 후 실제 사용하는 휴대폰 브라우저에서 보드가 1:1로 보이는지 한 번 최종 확인하는 것이 안전합니다.

## 최종 전달 ZIP 검증

최종 ZIP 생성 후 새 디렉터리에 다시 압축 해제하여 `npm run verify`를 재실행하고 ZIP 무결성 검사까지 완료한 뒤 아래 결과를 갱신합니다.
