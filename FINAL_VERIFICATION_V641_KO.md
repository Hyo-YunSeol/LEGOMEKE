# 레고 키우기 v6.4.1 최종 검증 결과

## 자동 검사 결과

- `npm run check`: JavaScript 33개 파일 문법 검사 통과.
- `npm run verify`: 총 156개 테스트 통과, 실패 0, 건너뜀 0.
- 최종 작업 코드에서 전체 `npm run verify`를 3회 연속 실행하여 모두 156/156 통과했습니다.
- 전달용 ZIP을 만든 뒤 별도 폴더에 다시 압축 해제하여 `npm run verify`를 실행했고 156/156, 실패 0으로 통과했습니다.
- ZIP 압축 무결성 검사(`unzip -t`)에서도 오류가 없습니다.

## v6.4.1 추가 검증

### 내 레고 게임 순위
- 레고방 순위의 포인트 TOP 5와 레벨 TOP 5 아래에 `게임 순위`가 존재하는지 확인.
- 홀짝 TOP 5, 사과게임 TOP 5, 오목 TOP 5가 한 영역에 함께 표시되는지 확인.
- 사과게임 화면과 오목 로비에서 별도 랭킹 중복 표시가 제거됐는지 확인.
- 홀짝 최고 연승 랭킹 데이터가 기존 사용자 데이터에서도 안전하게 생성되는지 확인.
- 사과 및 오목 기존 랭킹 정렬 규칙이 유지되는지 확인.

### 게임 화면 순서
- `포인트 개인게임`이 가장 먼저 표시되는지 확인.
- 그 아래 `단체게임` 구분이 표시되는지 확인.
- 단체게임 안에서 오목게임이 라이어게임보다 먼저 표시되는지 확인.

### 황금 영토 비공개
- 현재 시즌 종료 직전까지 `goldenRevealed=false`인지 확인.
- 현재 시즌 `territoryView.goldenCell`이 항상 `null`인지 확인.
- 현재 시즌 맵 셀의 `golden` 값이 항상 false인지 확인.
- v6.4.0 저장 상태에 `goldenRevealed=true`가 남아 있어도 현재 시즌은 비공개로 정상화되는지 확인.
- 시즌 종료 정산 이후 `lastResult.goldenCell`, 황금 영토 종료 시점 소유자가 공개되는지 확인.
- 황금 영토는 정산 시 기존 동률 승자 판정에만 사용되고 진행 중 순위에는 노출되지 않는지 확인.
- 다음 Durable Object 알람이 중간 공개 시간이 아니라 시즌 종료 시각을 사용하도록 확인.

### 운영자 포인트 지급/회수
- 비운영자는 `/api/admin/points`를 사용할 수 없도록 기존 관리자 권한 검사를 적용.
- 운영자 지급 요청이 대상 회원 포인트에 정확히 반영되는지 확인.
- 같은 `requestId`를 다시 보내면 `duplicate`로 처리되고 포인트가 다시 증가하지 않는지 확인.
- 회수 요청이 정확히 반영되는지 확인.
- 보유 포인트보다 많이 회수하려는 요청이 거부되는지 확인.
- 동일 포인트 지급 요청을 12개 동시에 보내도 정확히 한 번만 반영되는 동시성 테스트 통과.
- 관리 UI에 `포인트+`, `포인트-`가 존재하고 서버 API를 사용하도록 확인.

## 기존 기능 회귀검사

v6.4.0의 생활 행동, 포인트, 레고력/레벨, 파손/세대교체, 벙, 개인게임 30회, 사과게임, 낚시 20회, 매칭/커플/찌르기/교미, 오목, 라이어 및 관전, 영토전, 상태메시지, 운영자 기능, 저장소 재접속, 서비스워커/배포 설정 테스트를 함께 유지하여 전체 156개가 통과했습니다.

## 데이터 호환

- 저장 상태 메타 버전은 v15입니다.
- `adminPointOperations`가 없는 기존 상태에는 빈 객체를 기본값으로 적용합니다.
- `oddEvenBestAt`이 없는 기존 레고는 null로 보정하며 최고 연승 기록 자체는 유지합니다.
- 현재 시즌에 남아 있는 과거 황금 공개 플래그는 데이터 손실 없이 비공개 플래그로 정상화합니다.

## 운영환경 한계

이 환경에서는 실제 사용자의 Cloudflare 운영 계정에 직접 배포하거나 실제 iPhone/Android 기기를 손으로 조작하는 실기 테스트까지 수행할 수 없습니다. 자동 검사와 ZIP 재검증 통과와 별개로 배포 후 실제 브라우저에서 게임 순서, 관리자 포인트 버튼, 시즌 종료 후 황금 공개 화면을 한 번 확인하는 것을 권장합니다.
