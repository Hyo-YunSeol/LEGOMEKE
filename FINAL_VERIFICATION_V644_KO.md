# 레고 키우기 v6.4.4 최종 검증

## 이번 수정

- `README.md`, `README_FIRST.txt` 및 과거 버전의 중복 보고/요구/검증 문서를 제거했습니다.
- 현재 Cloudflare Worker 이름에 맞춰 `wrangler.jsonc`의 Worker 이름을 `legomeke`로 정리했습니다.
- Cloudflare Git 빌드용 명령은 `npm run verify`, 배포 명령은 `npx wrangler deploy`, Root directory는 저장소 루트 기준입니다.
- 영토전은 4×4 맵 16칸이 한 번이라도 모두 차면 해당 시즌 동안 `전면전` 상태를 유지합니다.
  - 전면전 전: 본진 보호.
  - 전면전 후: 본진 포함 모든 상대 영토 탈취 가능.
  - Lv1처럼 한 칸만 가진 사용자도 자기 본진을 옮기며 공격 가능.
  - 마지막 한 칸을 잃어 0칸이 되어도 다시 참가 가능.
  - 전면전 후 일시적으로 빈칸이 생겨도 본진 보호가 다시 켜지지 않음.
- 사과게임 모바일 숫자판에 `touch-action: none`, `overscroll-behavior: contain`, 비수동 `touchmove` 취소를 함께 적용해 드래그 중 페이지 스크롤 전달을 차단했습니다.
- 사과게임 포인트는 기존 확정값 그대로 유지합니다: 2개 제거 +5P, 3개 이상 제거 +6P.
- 낚시 UI의 `낚이는 것 보기`를 `낚시 보상 보기`, 팝업 제목을 `🎣 낚시 보상`으로 변경했습니다.
- 낚시 아이템/확률/보상표 자체는 아직 변경하지 않았습니다.
- API 진입부에서 Durable Object 바인딩 누락/진입 오류가 발생해도 가능한 경우 JSON 오류 응답을 반환하도록 방어했습니다.
- 클라이언트는 Cloudflare HTML 오류 같은 비JSON 응답을 기존의 모호한 `서버 응답을 읽을 수 없습니다`로 처리하지 않고 HTTP 상태와 Cloudflare/Durable Object 확인 안내를 표시합니다.
- 정적 파일 및 서비스워커 캐시는 v6.4.4(`v=644`)로 갱신했습니다.

## 자동 검증

최종 코드 기준 `npm run verify` 3회 연속 실행:

- JavaScript 문법 검사: 33개 파일 통과
- 전체 자동 테스트: 169개
- 성공: 169
- 실패: 0
- 스킵: 0

검증에는 기존 기능 회귀 테스트와 함께 다음 신규 테스트가 포함됩니다.

- 16/16칸 포화 시 전면전 자동 해제
- 1칸짜리 본진 탈취
- Lv1 본진 이동 공격
- 0칸 사용자 재진입
- 전면전 상태 지속
- 사과게임 모바일 스크롤 방어 코드
- 낚시 보상 문구
- 비JSON 서버 오류 구분
- Durable Object 바인딩 누락 시 JSON 오류 응답

## 제한

실제 Cloudflare 운영 계정에 직접 배포하거나 실제 iPhone/Android 브라우저에서 손으로 조작하는 실기 테스트는 이 실행 환경에서 수행할 수 없습니다. 자동 테스트와 코드 검증은 모두 통과했습니다. 배포 후 `/healthz`에서 `6.4.4-final`이 반환되는지 확인하고, 실제 휴대폰에서 사과게임 드래그 스크롤 차단을 한 번 확인하는 것이 최종 실기 점검입니다.
