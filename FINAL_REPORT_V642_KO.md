# 레고 키우기 v6.4.2 구현 보고

## 오목판

`public/styles.css`의 오목 보드를 15열·15행 Grid로 고정하고 보드 자체를 `aspect-ratio: 1 / 1`로 유지하도록 수정했습니다. 각 `.omok-cell`은 가로/세로 100%, 패딩 0, 브라우저 기본 appearance 제거를 적용했습니다. 모바일의 보드 폭은 화면에 맞춰 줄어들지만 높이는 1:1 비율에서 결정됩니다.

오목 서버 규칙, 판돈, 30초 턴, 금수, 관전, 승패 및 재대결 로직은 변경하지 않았습니다.

## 운영자 계정 완전 삭제

`public/app.js` 운영자 회원 관리에 `계정 삭제` 버튼과 2단계 확인을 추가했습니다. 운영자 본인 행에는 삭제 버튼을 표시하지 않습니다.

`src/worker.js`에는 `DELETE /api/admin/users/:userId`를 추가했습니다. 서버에서 `ADMIN_USER_IDS` 권한, 대상 존재 여부, 본인 삭제 여부를 확인한 뒤 삭제를 수행합니다.

삭제 처리에서는 대상 회원의 모든 세대 레고를 찾아 관계/찌르기/요청/개인게임 상태/영토/라이어/오목 상태를 정리하고 user/pet 레코드를 제거합니다. 삭제 직후 해당 userId의 WebSocket을 종료하며, 저장소에 user가 없으므로 기존 서명 토큰으로도 API 인증을 계속 사용할 수 없습니다.

오목 진행 중인 회원을 삭제하면 기존 서버 정산 함수를 통해 기권패로 처리해 상대에게 판돈을 한 번만 지급합니다. 라이어게임 진행 중 회원 삭제는 게임을 강제 안전 종료해 남은 플레이어의 미정산 판돈을 반환한 뒤 삭제합니다.

## 버전/캐시

- package: 6.4.2
- `/healthz`: `6.4.2-final`
- service worker cache: `lego-life-v642-final`
- CSS/JS asset query: `v=642`

최종 테스트 수치와 ZIP 재검증 결과는 `FINAL_VERIFICATION_V642_KO.md`에 기록합니다.
