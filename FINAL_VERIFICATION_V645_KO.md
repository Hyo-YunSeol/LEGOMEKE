# 레고 키우기 v6.4.5 최종 검증

## 이번 버전 핵심 수정

- 관리자 회원 목록을 현재 가입 계정(`users`) 기준으로 생성하도록 보강했습니다.
  - 관리자가 기존 계정을 완전 삭제한 뒤 같은 닉네임으로 새 계정이 재가입해도 새 `userId`의 회원으로 다시 표시됩니다.
  - 현재 레고 연결이 비정상인 계정도 회원 목록에서 사라지지 않으며, 수동 `회원 목록 새로고침`도 제공합니다.
- 게임 하루를 한국시간(KST) 공통 고정 경계인 `00:00 / 06:00 / 12:00 / 18:00`으로 명확히 통일했습니다.
  - 생활 행동, 개인게임, 낚시, 오늘의 레고력 등 기존 게임 하루 기반 초기화가 같은 경계를 사용합니다.
  - 구버전의 날짜 형식 저장값도 첫 전환에서 안전하게 호환합니다.
- 개인게임 전체 합산 이용 횟수를 게임 하루당 `40회`로 변경했습니다.
- 몸집 단계를 총 32단계로 확장했습니다.
  - 기존 `돼룩돼룩레고` 뒤에 `왕돼룩레고`, `괴물돼지레고`를 거쳐 공룡 단계로 진입합니다.
  - `프로토케라톱스 → 파키케팔로사우루스 → 카르노타우루스 → 안킬로사우루스 → 스테고사우루스 → 트리케라톱스 → ... → 브라키오사우루스 → 드레드노투스 → 파타고티탄 → 아르헨티노사우루스`까지 장기 성장 구간을 확보했습니다.
  - 긴 공룡 이름이 모바일에서 UI 밖으로 넘치지 않도록 줄바꿈을 보강했고, 새 단계용 SVG 자산을 추가했습니다.
- v6.4.4에서 반영한 영토전 전면전, 황금영토 종료 전 비공개, 사과게임 모바일 스크롤 방어, 사과게임 5P/6P, 오목 모바일 정사각형, 관리자 포인트/계정삭제/운영로그, 낚시 보상 문구, 서버 비JSON 오류 구분 등의 수정은 그대로 유지했습니다.
- 정적 파일과 서비스워커 캐시 버전을 `v645`로 갱신했습니다.

## 자동 검증

최종 코드 기준:

- JavaScript 문법 검사: 33개 파일 통과
- 전체 자동 테스트: 172개
- 성공: 172
- 실패: 0
- 스킵: 0

추가 검증 항목에는 다음이 포함됩니다.

- KST 00/06/12/18 경계의 `gameDayKey`와 다음 초기화 시각
- 39회 사용 상태에서 40번째 개인게임 완료 허용 및 41번째 시작 차단
- 삭제된 계정과 동일 닉네임으로 재가입한 새 계정이 관리자 목록에 다시 표시되는지 확인
- 32개 몸집 단계가 끊김 없이 이어지는지 확인
- 모든 새 몸집 단계 이미지 `assetKey` 존재 및 서비스워커 캐시 포함 여부
- 긴 공룡 단계명의 모바일 줄바꿈
- 기존 오목/라이어/사과/영토/낚시/관리자 기능 회귀 테스트

## 실기 확인 범위

실제 Cloudflare 운영 계정에 대한 배포와 실제 iPhone/Android 기기에서의 손가락 조작은 이 실행 환경에서 수행할 수 없습니다. 코드·서버 로직·정적 UI 구조에 대한 자동 검증은 모두 통과했습니다. 배포 후 `/healthz`에서 `6.4.5-final`을 확인하면 최신 Worker 배포 여부를 바로 확인할 수 있습니다.
