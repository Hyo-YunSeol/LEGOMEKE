# 레고 키우기 v6.4.0 최종 검증 결과

## 자동 검사 결과
- `npm run check`: JavaScript 33개 파일 문법 검사 통과.
- `npm run verify`: 총 152개 테스트 통과, 실패 0, 건너뜀 0.
- 최종 코드 상태에서 전체 `npm run verify`를 3회 연속 실행했고 152/152, 152/152, 152/152로 모두 통과했습니다.
- 전달용 ZIP을 생성한 뒤 별도 폴더에 다시 압축 해제하여 `npm run verify`를 재실행했고 152/152, 실패 0으로 동일하게 통과했습니다.

## 기존 기능 회귀검사
기존 v6.3.0 테스트를 유지한 상태에서 생활 행동, 포인트, 레고력/레벨, 파손/세대교체, 벙, 홀짝, 번개반응, 숫자맞히기, 낚시, 매칭/커플/찌르기/교미, 라이어게임, 영토전, 상태메시지, 운영자 권한, 저장소 재접속, 서비스워커/배포 설정을 함께 검사했습니다.

## v6.4.0 신규 검사
### 개인게임·사과게임
- 개인게임 전체 합산 30회, 낚시 20회 상수 확인.
- 사과 보드 10×10, 숫자 1~9 확인.
- 합 10만 제거, 합이 다르면 미제거, 제거 후 재생성 없음.
- 2개 제거 +2P, 3개 이상 제거 시 +3P 규칙 확인.
- 서버 보드/서버 포인트 계산 및 최종 1회 정산 확인.
- 동일 사과 선택 requestId를 12개 동시에 보내도 예정 포인트가 한 번만 올라가는지 확인.
- 클라이언트 요청에 임의 `reward: 999999`, `points: 999999`를 넣어도 반영되지 않는지 확인.
- 만료 후 여러 동시 부트스트랩에서도 포인트·이용 횟수·게임 기록이 한 번만 반영되는지 확인.
- 최고점이 더 높을 때만 갱신하는 흐름 확인.
- 사과게임 TOP 5가 한 판 최고점수 내림차순으로 정렬되고, 동점이면 해당 최고점수를 먼저 달성한 사용자가 앞서는지 확인.
- 모바일 좌표 코드에 실제 `elementFromPoint` 셀 판정, 보드 콘텐츠 박스 보정, Pointer Capture가 포함됐는지 정적 검사.

### 오목
- 판돈 100/500/1000/2000+ 1000단위 검증, 1500/2500 등 거부.
- 최대 3방 제한.
- 참가 시 양쪽 판돈 동시 확보.
- 관전자 착수 서버 차단.
- 가로/세로/좌상↔우하/우상↔좌하 승리 판정.
- 흑 정확한 5목, 장목 금지, 33 금지, 44 금지.
- 상대 돌에 막힌 가짜 33을 정상 수로 허용하는 케이스.
- 백 5목 이상 승리.
- 30초 서버 턴 진행 및 연속 3회 시간초과 패배.
- 승리 정산 후 동일 승리 착수 requestId 12개 동시 요청에서도 승자 포인트와 승/패 기록이 각각 정확히 한 번만 반영되는지 확인.
- 재대결 시 보드/턴/시간초과/승패/정산/요청 ID 초기화 및 양쪽 판돈 재확보 확인.
- 한쪽 포인트 부족 시 재대결 차단 및 추가 차감 없음.
- 오목 TOP 5가 승리 횟수 내림차순 → 패배 횟수 오름차순 → 무승부 횟수 내림차순의 일관된 기준으로 정렬되는지 확인.
- 오목 UI 15×15, 참가/관전/재대결 버튼과 오목 채팅 부재 정적 검사.

### 라이어게임 관전
- 관전자가 플레이어 수에 포함되지 않음.
- 진행 중 게임에 관전 API로 입장 가능.
- 관전자 응답의 `word`, `liarPetId`, `category`가 게임 종료 전 null인지 확인.
- 게임 종료 전 시스템 비밀 메시지가 관전자에게 전달되지 않는지 확인.
- 관전자 채팅 API 거부 및 게임 시작 권한 거부 확인.
- 관전자 투표/정답 등 기존 플레이어 입력 권한 미부여를 함수 단위로 확인.

### UI/가입 문구
- 게임 메뉴가 라이어게임 → 오목게임 → 개인게임 순서인지 확인.
- 사과게임 10열 CSS 및 모바일 `touch-action: none` 확인.
- 회원가입 버튼 바로 위 요청 문구가 정확히 존재하는지 확인.

## 동시성/중복 방어 근거
Cloudflare Durable Object의 기존 `runExclusive` 직렬 큐를 그대로 사용하며 사과/오목 API도 동일 큐를 통과합니다. 사과 선택은 requestId 기록, 오목 착수는 requestId 기록, 오목 결과는 `settled`/`settlementId`, 기존 게임들도 기존 완료/정산 플래그를 사용합니다.

## 데이터 호환
저장 상태 버전은 v14로 올리고 기존 사용자에게 없는 사과 최고점/오목 승무패/오목 상태/라이어 관전자 필드를 기본값으로 보정합니다. 기존 정상 기능 데이터 구조를 삭제하거나 전체 교체하지 않았습니다.

## 실기기/운영환경 한계
이 환경에서는 실제 운영 Cloudflare 계정에 배포하거나 실제 iPhone/Android 기기를 손으로 조작하는 실기 테스트까지 수행할 수 없습니다. 따라서 자동 검사 통과와 별개로 실제 배포 후 휴대폰 2대 이상에서 사과 드래그, 오목 동시 대전/관전, 라이어 관전을 한 번 확인하는 것을 권장합니다.
