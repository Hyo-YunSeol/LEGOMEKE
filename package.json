{
  "name": "lego-life-game",
  "version": "6.10.4",
  "private": true,
  "description": "레고방 포인트 경쟁 소셜 다마고치 + 블록게임 + 사과게임 + 오목게임 + 1:1 테트리스대전 + 1:1 사천성대전 + 2~4인 다빈치코드 + 5×5 영토전 - Cloudflare Workers v6.10.4",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check-all.mjs",
    "smoke:deploy": "node scripts/deploy-smoke.mjs",
    "build": "npm run check && npm run smoke:deploy",
    "test": "node scripts/run-core-tests.mjs",
    "test:browser": "node --test --test-concurrency=1 tests/browser-runtime.test.js",
    "lint": "eslint public/app.js public/sw.js",
    "verify": "npm run build && npm test",
    "verify:full": "npm run verify && npm run lint && npm run test:browser",
    "deploy:cloudflare": "npm run verify && npx --yes wrangler@4 deploy"
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "globals": "^16.4.0",
    "jsdom": "^26.1.0"
  }
}
