{
  "name": "lego-life-game",
  "version": "6.6.1",
  "private": true,
  "description": "레고방 포인트 경쟁 소셜 다마고치 + 음식 24종 + 상점 + 라이어 관전 + 사과게임 + 오목게임 + 5×5 영토전 - Cloudflare Workers v6.6.1",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check-all.mjs",
    "lint": "eslint public/app.js --max-warnings=0",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm run lint && npm test",
    "deploy:cloudflare": "npm run verify && npx wrangler deploy"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "globals": "^17.9.0",
    "jsdom": "^29.1.1"
  }
}
