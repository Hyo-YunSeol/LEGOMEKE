# 레고 키우기 v6.3.0 최종 검증 결과

> **기록용 문서:** 이 파일은 v6.3.0 기준 과거 기록입니다. 현재 최종본은 v6.4.0이며 `FINAL_REQUIREMENTS_V640_KO.md`와 `FINAL_VERIFICATION_V640_KO.md`를 우선 확인하세요.


## 자동 검사
- `scripts/check-all.mjs`: JavaScript 26개 파일 문법 검사 통과.
- 전체 `npm run verify`: 124개 테스트 통과, 실패 0, 건너뜀 0.
- 동일 전체 검사를 3회 연속 실행해 모두 통과.
- 동시접속 전용 테스트 7개를 10회 반복해 70/70 통과.
- 동시 동일 닉네임 가입, 동시 생활 행동, 같은 빈칸 동시 본진 점령, 낚시 중복 정산, 매칭 동시 수락, 오류 후 큐 복구, 홀짝 동시 시작을 검증.
- 동시접속 테스트 중 잘못된 JSON 요청을 일부러 보내는 케이스에서 서버가 오류 로그를 출력하지만 해당 테스트는 요청 오류 후 직렬화 큐가 정상 복구되는지 확인하는 의도된 검사입니다.

## 주요 규칙 회귀검사
- 홀짝: 최대 상한 없음, 10P 단위, 1승 1.3배/2승 1.6배/3승 2배, 중복 정산 차단.
- 숫자맞히기: 5회 제한, 정답 서버 은닉, 빈 입력/중복 완료 차단.
- 라이어: 1라운드, 3~12명, 게임 중 신규 참가 차단, 카테고리 참가자 공개, 라이어 제시어 은닉, 채팅 스크롤/입력 보존.
- 영토: 4×4, 첫 칸 본진, 본진 보호, 주변 8칸 확장·탈취, 한도 초과 시 오래된 일반 영토 자동 해제, 황금칸 종료 2시간 전 공개.
- 영토 API 점령/탈취 후 서버 응답의 보유칸·순위 재계산 확인.
- 체형: 10단계 및 맘모스/돼룩돼룩 이미지·서비스워커 캐시 확인.
- 체형별 게임성 행동 배고픔 -1/-2/-3/-4와 배고픔 0 하한 처리 확인.
- 벙: 정상 종료 보상과 최근 정상 종료 기록 10개 노출/저장 상한 확인.
- 상태메시지: 서버 20자 제한, 온라인/프로필 표시, 재접속 저장 확인.
- 레고 파손, 세대교체, 교미 궁합도/거절 50% 경고 및 소식, 삭제 기능 API 404 유지 확인.

## 커버리지
Node 내장 테스트 커버리지 기준:
- 전체 라인: 94.29%
- 전체 브랜치: 76.78%
- 전체 함수: 91.74%
- `engine.js` 라인: 95.73%
- `liar-game.js` 라인: 91.60%
- `territory.js` 라인: 99.36%

## 모바일 렌더 검사
Chromium의 실제 CSS 레이아웃 엔진에 프로젝트 `styles.css`를 주입해 320×568, 390×844, 430×932 크기로 확인했습니다.

영토전:
- 세 크기 모두 4행 × 각 4칸 = 16칸.
- 320px: 맵 256×256px, 문서 가로 320px.
- 390px: 맵 326×326px, 문서 가로 390px.
- 430px: 맵 360×360px, 문서 가로 430px.
- 세 크기 모두 가로 넘침 없음.

라이어 모바일 키보드 상태 CSS:
- 가상 키보드로 사용 가능한 화면 높이를 320px로 가정했을 때 채팅 섹션 높이 320px.
- 입력폼 하단 312px로 영역 안에 유지.
- 채팅 목록 `overflow-y: auto`, body `overflow: hidden` 확인.

체형 도감:
- 세 모바일 크기 모두 카드 10개, 현재 단계 강조 1개, 가로 넘침 없음.

주의: 이 Chromium 검사는 실제 iPhone Safari/Android Chrome 실기기가 아니라 Chromium 레이아웃 엔진을 사용한 모바일 크기 렌더 검사입니다.

## Cloudflare 배포 검사 한계
`wrangler@4.114.0 deploy --dry-run`을 시도했지만 이 실행환경의 내부 npm registry에 Wrangler 패키지가 없어 E404로 실행할 수 없었습니다. 따라서 실제 Cloudflare 계정 배포 자체와 실제 iPhone/Android 기기 네트워크 접속은 이 환경에서 검증하지 못했습니다.

배포 후 확인할 항목:
1. `/healthz`가 `6.3.0-final`을 반환하는지 확인.
2. 실제 휴대폰 2대 이상으로 동시 로그인 후 영토 점령/탈취와 라이어 채팅을 확인.
3. 휴대폰 키보드를 연 상태에서 다른 사용자가 라이어 채팅을 보내도 읽던 위치와 작성 중 문장이 유지되는지 확인.
4. 시즌 종료 2시간 전에만 황금 영토 위치가 공개되는지 운영 시간 기준으로 확인.

## 압축본 자체 재검사
초기 최종 ZIP을 새 폴더에 다시 압축 해제한 뒤 그 폴더에서 `npm run verify`를 실행했고 124/124 통과, 실패 0, 건너뜀 0을 확인했습니다. 최종 배포 파일을 다시 묶은 뒤에도 동일 검사를 한 번 더 수행해 124/124 통과, 실패 0, 건너뜀 0을 재확인했습니다.
